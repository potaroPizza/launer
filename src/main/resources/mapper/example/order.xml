<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.ez.launer.laundryService.order.model.OrderDAO">
	<sql id="orderListWhere">
		<where>
			<if test="customerId!=null and customerId!=''">
				customerId = #{customerId}
			</if>
			and REGDATE >= #{startDay}
			<![CDATA[
			and REGDATE < to_date(#{endDay})+1 ]]>
		</where>
	</sql>
	
    <select id="selectOrderList" parameterType="orderSearchVo" 
    	resultType="map">
       
    </select>
    
    <select id="selectTotalRecord" parameterType="orderSearchVo" resultType="int">
		select count(*) from orders
		<include refid="orderListWhere"></include>
	</select>
</mapper>